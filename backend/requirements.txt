fastapi==0.95.2
uvicorn[standard]==0.23.1
transformers==4.43.0
torch==2.6.0
optimum>=1.16.0
accelerate>=0.26.0
pydantic>=2.0.0

# Optional: for smaller installs you can replace torch with 'xformers' or use CPU-only wheels
# For even better performance on NVIDIA GPUs, also consider:
# flash-attn>=2.0.0  (requires CUDA and compatible GPU)